language: java
branches:
  only:
    - devlop
    - /^release-[0-999].[0-999]$/
    - master
notifications:
  email:
    recipients:
      - raul.24672@gmail.com
# services:
# - mysql
# - mongodb
# before_install:
# - mysql -e 'CREATE DATABASE IF NOT EXISTS forge;'
script:
  #Test unitario y de integracion con cobertura (jacoco)
  - mvn org.jacoco:jacoco-maven-plugin:prepare-agent verify



  #SONARCLOUD
  - mvn sonar:sonar -Dsonar.host.url=https://sonarcloud.io -Dsonar.organization=progress -Dsonar.login=$SONAR

#Deploy https://iwvg-ecosystem.herokuapp.com/api
deploy:
  provider: heroku
  api_key:
    secure: $HEROKU
  on:
    branch: master
#  app: iwvg-ecosystem, por defecto, nombre del artefacto
# ADD SONAR
